import * as React from "react"
import { Slot } from "@radix-ui/react-slot"
import { type VariantProps, cva } from "class-variance-authority"
import { cn } from "@/lib/utils"

// Since I don't have shadcn installed properly yet, I'll create a simple strict version
// First I need a utils file for cn
// I will create utils first in the next step, but for now I'll assume it exists or inline it?
// Better to create utils first.

// Wait, I will create utils.ts first.
